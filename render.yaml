services:
  - type: web
    name: moviehub-backend
    env: python
    buildCommand: pip install -e . -r requirements-web.txt
    startCommand: PYTHONPATH=src python -m uvicorn backend_app:app --host 0.0.0.0 --port $PORT
